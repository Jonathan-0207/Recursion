#ifndef REVERSE_H
#define REVERSE_H 

#include <string>

class Reverse{
    private:

    public:
        int reverseDigit(int value);
        std::string reverseString(std::string value);
}; 

#endif